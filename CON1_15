#include<bits/stdc++.h>
using namespace std;
int main()
{
    int q;
    cin>>q;
    cin.ignore();
    while(q--)
    {
        int x;string s;
        cin>>x>>s;
        if(next_permutation(s.begin(),s.end()))
        {
            cout<<x<<" "<<s<<endl;
        }else
        {
            cout<<x<<" "<<"BIGGEST"<<endl;
        }
    }
}
